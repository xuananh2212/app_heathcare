<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền -->
    <stroke
        android:width="1dp"
        android:color="@color/border_edittext" /> <!-- Màu và độ rộng của viền -->
    <corners android:radius="12dp" /> <!-- Bo góc, tùy chọn -->
    <padding
        android:left="8dp"
        android:top="0dp"
        android:right="8dp"
        android:bottom="0dp" /> <!-- Khoảng cách giữa viền và nội dung, tùy chọn -->
</shape>
